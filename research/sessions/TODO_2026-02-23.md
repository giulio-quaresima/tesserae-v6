# To-Do List — February 23, 2026

## Completed This Session (Feb 23)
- ~~Fix highlighting not working fully~~ — position-based highlighting + stem fallback
- ~~Fix rare word results returning non-rare words~~ — switched from token freq to document freq
- ~~Display lemmata instead of surface forms in rare word results~~
- ~~Fix rare pairs "Unexpected token" error~~ — added url_prefix='/api' to hapax blueprint
- ~~Fix "Bigram index not built" error~~ — symlinked production bigram indexes
- ~~Retry Shakespeare Midsummer embeddings~~ — downloaded, converted, embedded (2,171 lines)
- ~~Add result quality checks to test suite~~ — 7 new quality checks, 27/27 passing
- ~~Generate missing semantic embeddings~~ — completed for all texts

## Completed Feb 24
- ~~Proper noun filter (GitHub #6)~~ — ratio-based PN detection (50% threshold), local-context OR, conservative enclitic stripping (-que only), `clean_lemma_key()`, line text fallback. 119 PNs excluded, 0 capitalized non-PNs.
- ~~Timer formatting fix~~ — shared `formatElapsedTime()` in `formatting.js`, updated 6 locations across 5 components
- ~~Display capitalization fix~~ — lowercase first char of display_form for non-PNs to prevent line-initial leakage

## Completed Feb 25
- ~~Fix lemma matching only exact forms (GitHub #17)~~ — Sil. 6.186 ↔ Theb. 6.93 now matches. Three-layer fix: (1) `text_processor.py` strips CLTK trailing sense digits (`effero1`→`effero`); (2) added 43 missing *effero* forms to `latin_lemmas.json`; (3) corrected 209 lines in `syntax_latin.db`/`la_index.db` (LatinPipe `extuio`/`exfero`→`effero`); (4) cleaned 294K stale cached lemma entries across 1,393 Latin files; (5) added utility scripts `batch_lemma_cache.py`, `rebuild_lemma_cache.py`, `rebuild_embedding_manifest.py`
- ~~Phrase vs line unit type (GitHub #18)~~ — resolved by #17 fix. Phrase mode correctly combines lines across sentence boundaries.
- ~~Add sequential numbering to all search result types~~ — CorpusSearchResults, LineSearch, RareResultsDisplay, CrossLingualSearch, WildcardSearch now match SearchResults numbering style
- ~~Extending line matching (GitHub #19)~~ — closed, already implemented. V6 has phrase mode + max distance + fusion 2-line sliding window, exceeding V3's capabilities.
- ~~Implement `find_rare_word_matches_direct()` for fusion engine~~ — rare_word channel was silently broken (AttributeError caught, zero results). Added inverted-index function to `hapax.py`, lowered min_matches to 1. Production recall: 784/862 (91.0%), Achilleid-Thebaid 96.2% (+2 gold hits). Commit `66e55f2`.
- ~~Fix Greek diacritic normalization~~ — added `_normalize_lemma()` to `app.py`, applied to 3 search endpoints. Cleaned 22,000 leading-space entries + 926 empty entries from Greek inverted index. Commit `7e3ffcc`.
- ~~Enable AI semantic cross-lingual search~~ — changed `disabled={true}` to `disabled={searchLoading}` in `CrossLingualSearch.jsx`. Updated styling and label. Commit `7e3ffcc`.
- ~~Add cognate matching to cross-lingual highlighting~~ — 3-layer matching in `find_greek_latin_matches()`: curated pairs, V3 dictionary, Greek→Latin transliteration. Catches proper nouns (Menelaus, Cassandra, Sparta, etc.) and borrowed vocabulary. Commit `7e3ffcc`.

## Completed Feb 26
- ~~Stopword penalty + rare_word weight boost~~ — Pairs sharing only stoplist lemmas get 0.3× score penalty. rare_word channel weight raised 0.5→2.0. IDF-based rarity boost was attempted but removed (per-pair IDF too inflated for meaningful thresholds). Zero recall regression (784/862). Session: `research/sessions/2026-02-26_rarity_scoring.md`.

## Completed Feb 27
- ~~Weight optimization grid search~~ — Swept 34,992 weight configs + 25 bonus/penalty combos across 5 benchmarks (19.9 min). Config E found: R@500 improved 19.8%→22.6% (+24 gold in top 500). Key changes: edit_distance 4.0→2.0, sound 3.0→4.0, exact 2.0→1.0, lemma 1.5→2.0, rare_word 2.0→1.0, dictionary 1.0→1.5. Total recall unchanged (784/862, 91.0%). Study: `research/studies/2026-02-27_weight_optimization/REPORT.md`. Validation pending.

## Previously Completed (verified in code)
- ~~Make fusion the default search~~ — fusion is first option in dropdown
- ~~Add tooltip about large text search times~~ — shows under fusion dropdown
- ~~Review the 5,000 results cap~~ — "Top X of N Parallels" indicator implemented
- ~~Make button/UI colors consistent with red/amber theme~~ — zero green remaining
- ~~Make author/work required fields~~ — validation prevents empty searches
- ~~Fix lemma returning only exact matches (GitHub #17)~~ — fixed Feb 25: CLTK digit stripping + effero paradigm + index/cache cleanup
- ~~Fix phrase vs line unit type~~ — works correctly
- ~~Hide inapplicable advanced settings in fusion mode~~ — stoplist, max distance, etc. hidden

## Still Open

### 1. Fix some matched words not named
Some results show fewer matched words than expected. **Investigated Feb 24** — see `research/sessions/2026-02-24_investigation_matched_words.md`. Root causes: syntax channel contributes no match info (always `[]`), semantic fallback loses specific matches, sound/edit_distance lack `source_word`/`target_word` fields, fusion dedup loses variant forms.

### 2. Fix metrical boost in fusion mode
When metrical boost is applied to a fusion search, lemma search returns only low hundreds of results whereas V3 returned thousands. **Investigated Feb 24** — see `research/sessions/2026-02-24_investigation_metrical_boost.md`. Possible config merge bug at `fusion.py:862-864`. Needs debug logging to confirm.

### Low Priority (found during testing, not user-reported)

### ~~3. Greek inverted index cleanup~~
~~Leading spaces in lemma entries in the Greek inverted index prevent document frequency queries from matching.~~ **Fixed Feb 25** — cleaned 22,000 entries with leading spaces, deleted 926 empty entries. Part of commit `7e3ffcc`.

### ~~4. Greek corpus search diacritics~~
~~Frontend should auto-strip diacritics before querying Greek corpus search.~~ **Fixed Feb 25** — added `_normalize_lemma()` to backend that handles Greek diacritics (NFD + Mn filter + final sigma). Applied to line-search, corpus-search, and line-search-parallel endpoints. Part of commit `7e3ffcc`.

### 5. Cross-lingual highlighting — further improvements
Cognate matching via transliteration added to `find_greek_latin_matches()` (commit `7e3ffcc`). Future: word-level SPhilBERTa embedding alignment for semantic_cross mode, V3 dictionary expansion.

## Notes
- All optimizations from Feb 22 are live on the dev server (port 8080)
- Cache was cleared — first searches will be slower (no cache hits)
- Benchmark recall confirmed at 784/862 (91.0%) after all changes
- Dev server running in tmux session `devserver`
- 27/27 endpoint tests passing (including 7 quality checks)
- Full code quality audit written: `research/sessions/2026-02-24_code_quality_audit.md`
